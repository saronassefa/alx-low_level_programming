#include "main.h"
/**
*int _islower-checks lower case
*
*Return: 1 & 0
*/
int _islower(int )
{
char n;
if (n >= 'a' && n <= 'z')
{
return (1);
}
else if (n >= 'A' && n <= 'Z')
{
return (0);
}
 return (0);
}
