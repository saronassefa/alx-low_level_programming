#include "main.h"
/**
*_isalpha- checks alpha
*@c: ascii
*
*Return: zero and one
*/
int _isalpha(int c)
{
if (c >= 'a' && c <= 'z' && c >= 'A' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
return (0);
}
